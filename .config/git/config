[pager]
  # diff so fancy
  diff = diff-so-fancy | less --tabs=4 -RFX
  show = diff-so-fancy | less --tabs=4 -RFX

[help]
  autocorrect = 1

[push]
  default = current
  followTags = true

[fetch]
  prune = true

[log]
  date = relative
